<!-- Generated by box-gantry. DO NOT EDIT. -->
# UsersManager

Access via `client.NewClient().Users`.

## List

`GET /users`

| Parameter | In | Type | Required |
|---|---|---|---|
| `filter_term` | query | `string` | no |
| `user_type` | query | `GetUserType` | no |
| `external_app_user_id` | query | `string` | no |
| `fields` | query | `[]string` | no |
| `offset` | query | `int64` | no |
| `limit` | query | `int64` | no |
| `usemarker` | query | `bool` | no |
| `marker` | query | `string` | no |

**Returns:** `Users`

**Example**

```go
for item, err := range client.Users.List(context.Background(), nil) {
	if err != nil {
		log.Fatal(err)
	}
	fmt.Println(item)
}
```

Paginated — `List(...)` returns `iter.Seq2[*T, error]`, threading the cursor for you. See the [pagination guide](../pagination.md).

## Create

`POST /users`

| Parameter | In | Type | Required |
|---|---|---|---|
| `fields` | query | `[]string` | no |

**Request body** (`application/json`): `UserCreateRequest`

**Returns:** `UserFull`

**Example**

```go
result, err := client.Users.Create(context.Background(), &schemas.UserCreateRequest{}, nil)
if err != nil {
	log.Fatal(err)
}
fmt.Println(result)
```

## GetMe

`GET /users/me`

| Parameter | In | Type | Required |
|---|---|---|---|
| `fields` | query | `[]string` | no |

**Returns:** `UserFull`

**Example**

```go
result, err := client.Users.GetMe(context.Background(), nil)
if err != nil {
	log.Fatal(err)
}
fmt.Println(result)
```

## Get

`GET /users/{user_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `user_id` | path | `string` | yes |
| `fields` | query | `[]string` | no |

**Returns:** `UserFull`

**Example**

```go
result, err := client.Users.Get(context.Background(), "USER_ID", nil)
if err != nil {
	log.Fatal(err)
}
fmt.Println(result)
```

## Update

`PUT /users/{user_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `user_id` | path | `string` | yes |
| `fields` | query | `[]string` | no |

**Request body** (`application/json`): `UserUpdateRequest`

**Returns:** `UserFull`

**Example**

```go
result, err := client.Users.Update(context.Background(), "USER_ID", &schemas.UserUpdateRequest{}, nil)
if err != nil {
	log.Fatal(err)
}
fmt.Println(result)
```

## Delete

`DELETE /users/{user_id}`

| Parameter | In | Type | Required |
|---|---|---|---|
| `user_id` | path | `string` | yes |
| `notify` | query | `bool` | no |
| `force` | query | `bool` | no |

**Returns:** no content

**Example**

```go
if err := client.Users.Delete(context.Background(), "USER_ID", nil); err != nil {
	log.Fatal(err)
}
```

